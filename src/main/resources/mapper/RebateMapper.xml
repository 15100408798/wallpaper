<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yushang.wallpaper.common.mapper.RebateMapper">
    <update id="deleteRB">
        update tb_rebate set delete_flag = 1 where rebate_id in
        <foreach collection="array" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </update>
    <select id="selectFKList" resultType="java.util.HashMap">
        select
          rebate_id*1 as rebateId,rebate_name as rebateName,gradel*1 as gradel,create_time as createTime
        from tb_rebate where delete_flag = 0 order by rebate_id desc
    </select>
    <select id="selectCount" resultType="java.lang.Integer">
        select count(1) from tb_rebate where delete_flag = 0
    </select>
</mapper>